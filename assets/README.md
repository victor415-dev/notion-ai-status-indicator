# assets

以下二进制素材为运行期所需，请自行放置（占位，仓库暂未包含）：

- `icon-16.png` / `icon-48.png` / `icon-128.png` —— 扩展图标与通知图标。
- `ding.mp3` —— 任务完成提示音（M3，可选）。

> 缺少 `icon-128.png` 时系统通知会静默失败（代码已吞掉报错，不影响其余功能）；
> 缺少 `ding.mp3` 时不播放提示音。**M1（状态检测）不依赖这些素材。**
